"use strict";

const globalConfig = {
  customLinkStyles: !true,
  linksOldSettings: {
    redirectToNew: false,
    showUpdateMsg: true
  },
  xitter: true
};

export default globalConfig;